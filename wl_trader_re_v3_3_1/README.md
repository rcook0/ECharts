# WL Trader (RE) v3.3.1
Delta: Windows PowerShell deploy, CCXT smoke, MT5 ZMQ stub, pytest suite, env interpolation in compose.
Quickstart:
```bash
docker compose up --build
# http://localhost:8080
# Admin: admin@local / admin123
# User : demo@user.local / demo123
```
Windows: see deploy/win/deploy.ps1
